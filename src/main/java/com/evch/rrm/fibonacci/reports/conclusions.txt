Рекурсивный метод расчета - самый времязатратный, что начинает проявлятся примерно со значения 40.
Для 45 время на на вычисление порядка 7-8с.
Сложность выполнения O(2^n)

Рекурсивный с кэшем считается быстро. Проблема может возникнуть с переполнением стэка вызовов,
требует увеличепния памяти под стек.
Сложность выполнения скорее O(nlogn), или O(n^2)

Итеративный самый быстрый и время- и памятиэкономный.
Сложность выполнения O(n).

Тест на память самый недостоверный, т.к. ее размер напрямую зависит от работы неконтроллируемого GC.

